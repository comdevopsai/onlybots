name: Auto-merge whitelisted PRs

# Merges PRs into protected `main` once they are approved.
# Runs with the workflow's GITHUB_TOKEN (contents: write + pull-requests: write),
# which is the sanctioned path for merging into branch-protected refs.
on:
  pull_request_review:
    types: [submitted]
  pull_request:
    types: [labeled, opened, reopened, synchronize]

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    # Only act on the target repo's own PRs (not forks) and approved reviews.
    if: >-
      github.event.pull_request.base.ref == 'main' &&
      github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - name: Whitelisted author check
        id: check
        env:
          AUTHOR: ${{ github.event.pull_request.user.login }}
          REVIEW_STATE: ${{ github.event.review.state }}
        run: |
          WHITELIST="schneidergithub"
          echo "PR author: $AUTHOR"
          if [ "$AUTHOR" != "$WHITELIST" ]; then
            echo "Author '$AUTHOR' is not whitelisted ($WHITELIST). Skipping."
            echo "proceed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # If triggered by a review, require it to be an approval.
          if [ "$REVIEW_STATE" = "approved" ] || [ -z "$REVIEW_STATE" ]; then
            echo "proceed=true" >> "$GITHUB_OUTPUT"
          else
            echo "Review state '$REVIEW_STATE' is not an approval. Skipping."
            echo "proceed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Enable auto-merge
        if: steps.check.outputs.proceed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_URL: ${{ github.event.pull_request.html_url }}
        run: |
          gh pr merge --auto --squash "$PR_URL"
